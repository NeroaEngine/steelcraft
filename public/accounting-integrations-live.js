(function(){
const PATH='/portal/accounting',KEY='steelcraft_accounting_integrations_v1',CTX='steelcraft_business_provider_setup_context',EV='steelcraft-business-provider-setup';
const providers=[
{id:'quickbooks_online',name:'QuickBooks Online',type:'OAuth2',route:'/connect/business/quickbooks_online/setup',auth:'/connect/business/quickbooks_online/authorize',status:'/connect/business/quickbooks_online/status',test:'/connect/business/quickbooks_online/test-connection'},
{id:'quickbooks_desktop_enterprise',name:'QuickBooks Desktop / Enterprise',type:'Desktop connector / file bridge',route:'/connect/business/quickbooks_desktop_enterprise/setup',auth:'/connect/business/quickbooks_desktop_enterprise/authorize',status:'/connect/business/quickbooks_desktop_enterprise/status',test:'/connect/business/quickbooks_desktop_enterprise/test-connection'},
{id:'foundation_software',name:'Foundation Software',type:'Construction accounting ERP',route:'/connect/business/foundation_software/setup',auth:'/connect/business/foundation_software/authorize',status:'/connect/business/foundation_software/status',test:'/connect/business/foundation_software/test-connection'}
];
const bankSeed=[['Operating Checking','**** 8142','Ready for import preview'],['Payroll Checking','**** 1027','Authorization required'],['Savings / Reserve','**** 4491','Ready for import preview']];
const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function load(){try{return JSON.parse(localStorage.getItem(KEY))||{selected:'quickbooks_online',panel:'overview',providers:{},bankAccounts:bankSeed,log:[]}}catch{return{selected:'quickbooks_online',panel:'overview',providers:{},bankAccounts:bankSeed,log:[]}}}
let state=load();function save(){localStorage.setItem(KEY,JSON.stringify(state))}function provider(id){return providers.find(p=>p.id===id)||providers[0]}function pstate(id){state.providers[id]=state.providers[id]||{status:'setup_required',lastAction:'Not started',vaultRef:'',preview:[]};return state.providers[id]}
function css(){if(document.getElementById('acct-integrations-style'))return;const s=document.createElement('style');s.id='acct-integrations-style';s.textContent='.acct-integrations{display:grid;gap:12px;margin-bottom:14px}.acct-integration-hero{border:1px solid var(--line);border-radius:18px;background:rgba(255,255,255,.04);padding:14px;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px}.acct-integration-hero h2{margin:0;font-size:22px}.acct-integration-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.acct-integration-card,.acct-integration-panel{border:1px solid var(--line);border-radius:16px;background:rgba(255,255,255,.035);padding:12px;display:grid;gap:8px}.acct-integration-card h3{margin:0;font-size:15px}.acct-integration-card p,.acct-integration-panel p{margin:0;color:var(--muted);font-size:12px}.acct-integration-actions{display:flex;gap:7px;flex-wrap:wrap}.acct-int-btn{border:1px solid var(--line);border-radius:999px;background:rgba(255,255,255,.06);color:var(--text);padding:7px 10px;font-size:11px;font-weight:900;cursor:pointer}.acct-int-btn.primary{border:0;background:var(--brand-accent);color:#fff}.acct-int-pill{display:inline-flex;width:fit-content;border:1px solid var(--line);border-radius:999px;padding:4px 8px;color:var(--muted);font-size:10px;font-weight:900}.acct-int-table{display:grid;border:1px solid var(--line);border-radius:14px;overflow:hidden}.acct-int-row{display:grid;grid-template-columns:1fr 1fr 1fr;border-bottom:1px solid var(--line);min-height:38px}.acct-int-row>*{padding:8px 10px;border-right:1px solid var(--line);font-size:12px}.acct-int-head{background:rgba(0,0,0,.22);color:var(--muted);text-transform:uppercase;letter-spacing:.08em;font-size:10px;font-weight:950}@media(max-width:1100px){.acct-integration-grid{grid-template-columns:1fr 1fr}}@media(max-width:700px){.acct-integration-grid{grid-template-columns:1fr}.acct-integration-hero{grid-template-columns:1fr}}';document.head.appendChild(s)}
function log(msg){state.log.unshift(`${new Date().toLocaleTimeString()} - ${msg}`);state.log=state.log.slice(0,8);save()}
function context(id,source){const p=provider(id),ps=pstate(id);const payload={provider_id:p.id,provider_name:p.name,setup_status:ps.status,setup_url:p.route,account_id:null,contact_id:null,source:source||'accounting'};localStorage.setItem(CTX,JSON.stringify(payload));window.dispatchEvent(new CustomEvent(EV,{detail:payload}));return payload}
function run(action,id){const p=provider(id),ps=pstate(id);state.selected=id;state.panel='detail';if(action==='setup'){ps.status='setup_started';ps.lastAction='Setup opened';context(id,'accounting_setup');history.pushState({},'',p.route);window.dispatchEvent(new PopStateEvent('popstate'));log(`Opened setup for ${p.name}`)}if(action==='authorize'){ps.status='authorization_required';ps.lastAction='Customer authorization required';context(id,'accounting_authorize');log(`Prepared authorization for ${p.name}`)}if(action==='test'){ps.status=ps.status==='connected'?'connected':'setup_required';ps.lastAction=ps.status==='connected'?'Connection test passed':'Connect provider before test';log(`${p.name}: ${ps.lastAction}`)}if(action==='connect-local'){ps.status='connected';ps.lastAction='Connection reference stored';ps.vaultRef=`vault-provider-${p.id}-${Date.now()}`;context(id,'accounting_connect');log(`${p.name} connection reference stored`)}if(action==='preview'){ps.preview=[['Chart of accounts','Ready to map','Approval required'],['Customers / Vendors','Ready to scan','Approval required'],['Bank accounts','Ready to preview','No money movement'],['Open invoices / bills','Ready to preview','Approval required']];ps.lastAction='Migration preview prepared';log(`${p.name} migration preview prepared`)}if(action==='banks'){ps.status=ps.status==='connected'?'connected':'authorization_required';state.bankAccounts=bankSeed.map((r,i)=>[r[0],r[1],ps.status==='connected'?'Preview ready':'Authorization required']);ps.lastAction='Bank account import preview updated';log(`${p.name} bank account import preview updated`)}save();render()}
function card(p){const ps=pstate(p.id);return `<article class="acct-integration-card"><span class="acct-int-pill">${esc(p.type)}</span><h3>${esc(p.name)}</h3><p>Status: ${esc(ps.status.replace(/_/g,' '))}</p><p>${esc(ps.lastAction)}</p><div class="acct-integration-actions"><button class="acct-int-btn primary" data-acct-action="setup" data-provider="${p.id}">Open Setup</button><button class="acct-int-btn" data-acct-action="connect-local" data-provider="${p.id}">Store Connection Ref</button><button class="acct-int-btn" data-acct-action="banks" data-provider="${p.id}">Pull Bank Accounts</button><button class="acct-int-btn" data-acct-action="preview" data-provider="${p.id}">Migration Preview</button></div></article>`}
function detail(){const p=provider(state.selected),ps=pstate(p.id);return `<section class="acct-integration-panel"><h3>${esc(p.name)} integration workspace</h3><p>Provider route: ${esc(p.route)}</p><div class="acct-integration-actions"><button class="acct-int-btn primary" data-acct-action="authorize" data-provider="${p.id}">Authorize Provider</button><button class="acct-int-btn" data-acct-action="test" data-provider="${p.id}">Test Connection</button><button class="acct-int-btn" data-acct-action="banks" data-provider="${p.id}">Refresh Bank Accounts</button><button class="acct-int-btn" data-acct-action="preview" data-provider="${p.id}">Build Migration Preview</button></div><div class="acct-int-table"><div class="acct-int-row acct-int-head"><b>Import area</b><b>Status</b><b>Control</b></div>${(ps.preview.length?ps.preview:[['Connection','Waiting for setup','Customer approval'],['Bank accounts','Waiting for authorization','No money movement'],['Accounting migration','Waiting for preview','Approval required']]).map(r=>`<div class="acct-int-row"><strong>${esc(r[0])}</strong><span>${esc(r[1])}</span><b>${esc(r[2])}</b></div>`).join('')}</div></section>`}
function banks(){return `<section class="acct-integration-panel"><h3>Bank account import preview</h3><p>Bank connections only prepare import previews here. No transfers or money movement.</p><div class="acct-int-table"><div class="acct-int-row acct-int-head"><b>Account</b><b>Masked number</b><b>Status</b></div>${state.bankAccounts.map(r=>`<div class="acct-int-row"><strong>${esc(r[0])}</strong><span>${esc(r[1])}</span><b>${esc(r[2])}</b></div>`).join('')}</div></section>`}
function render(){if(!location.pathname.startsWith(PATH))return;css();const workspace=document.querySelector('.workspace');if(!workspace)return;let mount=document.getElementById('acct-integrations-live');if(!mount){mount=document.createElement('section');mount.id='acct-integrations-live';workspace.prepend(mount)}mount.className='acct-integrations';mount.innerHTML=`<header class="acct-integration-hero"><div><p class="eyebrow">Neroa Connect Accounting</p><h2>Accounting migrations and integrations</h2><p>Connect QuickBooks Online, QuickBooks Desktop / Enterprise, Foundation Software, and bank account import previews from Accounting.</p></div><span class="acct-int-pill">Clickable integration layer</span></header><div class="acct-integration-grid">${providers.map(card).join('')}</div>${detail()}${banks()}${state.log.length?`<section class="acct-integration-panel"><h3>Integration activity</h3>${state.log.map(x=>`<p>${esc(x)}</p>`).join('')}</section>`:''}`;bind(mount)}
function bind(mount){mount.querySelectorAll('[data-acct-action]').forEach(btn=>{btn.onclick=e=>{e.preventDefault();e.stopPropagation();run(btn.dataset.acctAction,btn.dataset.provider)}})}
window.addEventListener('popstate',()=>setTimeout(render,80));new MutationObserver(()=>setTimeout(render,120)).observe(document.documentElement,{childList:true,subtree:true});setTimeout(render,120);setTimeout(render,800);
})();
