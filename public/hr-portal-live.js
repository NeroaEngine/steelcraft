(function () {
  const PATH = '/portal/hr';
  const KEY = 'steelcraft_hr_live_v6';
  const stages = ['Profile', 'Documents', 'Training', 'Payroll', 'Ready'];
  const emptyEmployee = { name: '', role: '', department: '', startDate: '', status: 'Onboarding', email: '', phone: '' };
  const emptyTraining = { title: '', owner: '', dueDate: '', audience: 'All employees', status: 'Draft' };
  const emptyPolicy = { title: '', category: '', owner: '', status: 'Draft', summary: '' };
  const seed = {
    tab: 'employees',
    message: '',
    draftEmployee: { ...emptyEmployee },
    draftTraining: { ...emptyTraining },
    draftPolicy: { ...emptyPolicy },
    employees: [
      { id: 'emp-colton', name: 'Colton Cameron', role: 'Head Project Manager / Estimator', department: 'Project Management', startDate: '', status: 'Active', email: 'colton@mariondevelopmentgroup.com', phone: '13523611818', onboarding: { Profile: true, Documents: true, Training: true, Payroll: true, Ready: true }, training: {}, policies: { 'Employee Handbook': true } },
      { id: 'emp-seth', name: 'Seth McBride', role: 'President', department: 'Development', startDate: '2022-01-01', status: 'Active', email: 'seth@mariondevelopmentgroup.com', phone: '13522869466', onboarding: { Profile: true, Documents: true, Training: true, Payroll: true, Ready: true }, training: {}, policies: { 'Employee Handbook': true } },
      { id: 'emp-bj', name: 'BJ Hammett', role: 'Partner', department: 'Sales', startDate: '2023-10-01', status: 'Active', email: '', phone: '', onboarding: { Profile: true, Documents: true, Training: true, Payroll: true, Ready: true }, training: {}, policies: { 'Employee Handbook': true } },
      { id: 'emp-brad', name: 'Brad Hammett', role: 'Partner', department: 'Development', startDate: '2023-10-01', status: 'Active', email: 'brad@mariondevelopmentgroup.com', phone: '13524278803', onboarding: { Profile: true, Documents: true, Training: true, Payroll: true, Ready: true }, training: {}, policies: { 'Employee Handbook': true } }
    ],
    training: [
      { id: 'tr-safety', title: 'Safety Orientation', owner: 'HR / Safety', dueDate: '2026-07-01', audience: 'All employees', status: 'Active' },
      { id: 'tr-fall', title: 'Fall Protection', owner: 'Safety', dueDate: '2026-07-15', audience: 'Field employees', status: 'Active' },
      { id: 'tr-erp', title: 'Neroa ERP Basics', owner: 'Admin', dueDate: '2026-07-10', audience: 'Office employees', status: 'Draft' }
    ],
    policies: [
      { id: 'pol-handbook', title: 'Employee Handbook', category: 'General HR', owner: 'HR', status: 'Published', summary: 'Company standards, attendance, conduct, and employee acknowledgements.' },
      { id: 'pol-ppe', title: 'PPE Policy', category: 'Safety', owner: 'Safety', status: 'Published', summary: 'Required PPE and jobsite safety rules.' },
      { id: 'pol-timecard', title: 'Timecard Policy', category: 'Payroll', owner: 'Accounting', status: 'Draft', summary: 'Clock-in, approval, corrections, and payroll cutoff.' }
    ]
  };
  let state = null;
  let root = null;
  const clone = (value) => JSON.parse(JSON.stringify(value));
  const esc = (value) => String(value || '').replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]));
  const uid = (prefix) => `${prefix}-${Date.now()}-${Math.floor(Math.random() * 1000)}`;
  function load() { try { return { ...clone(seed), ...(JSON.parse(localStorage.getItem(KEY)) || {}) }; } catch { return clone(seed); } }
  function save() { localStorage.setItem(KEY, JSON.stringify(state)); }
  function percent(employee) { return Math.round((stages.filter((stage) => employee.onboarding && employee.onboarding[stage]).length / stages.length) * 100); }
  function currentStage(employee) { let active = 'Profile'; stages.forEach((stage) => { if (employee.onboarding && employee.onboarding[stage]) active = stage; }); return active; }
  function setStage(employee, stage) { const index = stages.indexOf(stage); employee.onboarding = employee.onboarding || {}; stages.forEach((item, idx) => { employee.onboarding[item] = idx <= index; }); employee.status = stage === 'Ready' ? 'Active' : 'Onboarding'; }
  function assignedCount(trainingTitle) { return state.employees.filter((employee) => employee.training && employee.training[trainingTitle]).length; }
  function css() {
    if (document.getElementById('hr-live-v6-style')) return;
    const style = document.createElement('style');
    style.id = 'hr-live-v6-style';
    style.textContent = `.hr-live-v6{width:calc(100vw - 44px);display:grid;gap:12px;font-size:12px}.hr-hero{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:start}.hr-hero h1{margin:2px 0;font-size:30px;line-height:1}.hr-hero p{margin:0;color:var(--muted);font-size:12px}.hr-tabs,.hr-actions{display:flex;gap:7px;flex-wrap:wrap}.hr-tabs button,.hr-btn{border:1px solid var(--line);border-radius:999px;background:rgba(255,255,255,.06);color:var(--text);padding:6px 9px;font-size:11px;font-weight:900;cursor:pointer}.hr-tabs button.active,.hr-btn.primary{border:0;background:var(--brand-accent);color:#fff}.hr-message{border:1px solid var(--line);border-radius:12px;background:rgba(255,255,255,.04);padding:8px 10px;color:var(--text);font-size:12px}.hr-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px}.hr-stat,.hr-panel,.hr-stage{border:1px solid var(--line);border-radius:14px;background:rgba(255,255,255,.035);padding:10px}.hr-stat strong{display:block;color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.08em}.hr-stat b{display:block;color:var(--text);font-size:22px;margin-top:1px}.hr-stat span{font-size:10px;color:var(--muted)}.hr-form{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:7px;align-items:end}.hr-form label,.hr-cell label{display:grid;gap:3px;color:var(--muted);font-size:10px;font-weight:900}.hr-form input,.hr-form select,.hr-table input,.hr-table select{width:100%;min-height:31px;border:1px solid rgba(255,255,255,.13);border-radius:8px;background:rgba(0,0,0,.22);color:var(--text);padding:5px 7px;font-size:11px;font-weight:800;box-sizing:border-box}.hr-table{display:grid;border:1px solid var(--line);border-radius:14px;background:rgba(255,255,255,.025);overflow-x:auto}.hr-row{min-width:1120px;display:grid;grid-template-columns:1fr .8fr .8fr .7fr .7fr 1fr .65fr;border-bottom:1px solid var(--line)}.hr-row.training{grid-template-columns:1.2fr .75fr .75fr .95fr .65fr .7fr .7fr}.hr-row.policy{grid-template-columns:1.15fr .7fr .7fr .65fr 1.35fr .65fr}.hr-row.head{background:rgba(0,0,0,.22);color:var(--muted);text-transform:uppercase;letter-spacing:.08em;font-size:10px;font-weight:950}.hr-cell{min-height:42px;padding:7px 9px;border-right:1px solid var(--line);display:grid;align-items:center}.hr-cell strong,.hr-card strong{font-size:12px}.hr-cell small,.hr-card small{font-size:10px;color:var(--muted)}.hr-pill{display:inline-flex;width:fit-content;border-radius:999px;padding:3px 7px;border:1px solid var(--line);background:rgba(255,255,255,.06);color:var(--muted);font-size:10px;font-weight:900}.hr-board{display:grid;grid-template-columns:repeat(5,minmax(160px,1fr));gap:8px}.hr-stage{min-height:280px;display:grid;gap:7px;align-content:start}.hr-stage.drag-over{outline:2px solid var(--brand-accent);background:rgba(159,61,66,.16)}.hr-stage h3{margin:0;display:flex;justify-content:space-between;gap:8px;align-items:center;font-size:13px}.hr-drop-note{margin:0;color:var(--muted);font-size:10px}.hr-card{border:1px solid rgba(255,255,255,.14);border-radius:12px;background:rgba(0,0,0,.22);padding:8px;display:grid;gap:5px;cursor:grab}.hr-card:active{cursor:grabbing}.hr-card .hr-actions button{padding:4px 7px}.hr-muted{color:var(--muted)}@media(max-width:1100px){.hr-stats,.hr-board{grid-template-columns:1fr 1fr}.hr-form{grid-template-columns:1fr 1fr}}@media(max-width:720px){.hr-stats,.hr-board,.hr-form{grid-template-columns:1fr}}`;
    document.head.appendChild(style);
  }
  function field(group, key, label, options, type) { const value = state[group][key] || ''; if (options) return `<label>${label}<select data-draft="${group}.${key}">${options.map((option) => `<option${value === option ? ' selected' : ''}>${option}</option>`).join('')}</select></label>`; return `<label>${label}<input ${type ? `type="${type}"` : ''} data-draft="${group}.${key}" value="${esc(value)}" /></label>`; }
  function form(group, fields, action, title) { return `<section class="hr-panel"><h2>${title}</h2><div class="hr-form">${fields.join('')}<button type="button" class="hr-btn primary" data-action="${action}">Create</button></div></section>`; }
  function employeesView() { return `${form('draftEmployee', [field('draftEmployee','name','Name'), field('draftEmployee','role','Role'), field('draftEmployee','department','Department'), field('draftEmployee','startDate','Start',null,'date'), field('draftEmployee','status','Status',['Onboarding','Active','Inactive']), field('draftEmployee','email','Email')], 'add-employee', 'Create employee')}<section class="hr-table"><div class="hr-row head"><div class="hr-cell">Employee</div><div class="hr-cell">Role</div><div class="hr-cell">Dept</div><div class="hr-cell">Start</div><div class="hr-cell">Status</div><div class="hr-cell">Email / Phone</div><div class="hr-cell">Progress</div></div>${state.employees.map((e) => `<div class="hr-row"><div class="hr-cell"><strong>${esc(e.name)}</strong></div><div class="hr-cell"><input data-emp="${e.id}" data-prop="role" value="${esc(e.role)}" /></div><div class="hr-cell"><input data-emp="${e.id}" data-prop="department" value="${esc(e.department)}" /></div><div class="hr-cell"><input type="date" data-emp="${e.id}" data-prop="startDate" value="${esc(e.startDate)}" /></div><div class="hr-cell"><select data-emp="${e.id}" data-prop="status"><option${e.status === 'Onboarding' ? ' selected' : ''}>Onboarding</option><option${e.status === 'Active' ? ' selected' : ''}>Active</option><option${e.status === 'Inactive' ? ' selected' : ''}>Inactive</option></select></div><div class="hr-cell"><small>${esc(e.email)}<br/>${esc(e.phone)}</small></div><div class="hr-cell"><span class="hr-pill">${percent(e)}%</span></div></div>`).join('')}</section>`; }
  function onboardingView() { return `<div class="hr-board">${stages.map((stage) => { const cards = state.employees.filter((e) => currentStage(e) === stage); return `<section class="hr-stage" data-stage="${esc(stage)}"><h3>${esc(stage)} <span class="hr-pill">${cards.length}</span></h3><p class="hr-drop-note">Drag cards into this column.</p>${cards.map((e) => `<article class="hr-card" draggable="true" data-emp-card="${e.id}"><strong>${esc(e.name)}</strong><small>${esc(e.role)} - ${esc(e.department)}</small><span class="hr-pill">${percent(e)}% complete</span><div class="hr-actions"><button type="button" class="hr-btn" data-action="move-prev" data-emp="${e.id}">Back</button><button type="button" class="hr-btn primary" data-action="move-next" data-emp="${e.id}">Next</button></div></article>`).join('')}</section>`; }).join('')}</div>`; }
  function trainingView() { return `${form('draftTraining', [field('draftTraining','title','Training'), field('draftTraining','owner','Owner'), field('draftTraining','dueDate','Due',null,'date'), field('draftTraining','audience','Audience'), field('draftTraining','status','Status',['Draft','Active','Archived'])], 'add-training', 'Create training')}<section class="hr-table"><div class="hr-row training head"><div class="hr-cell">Training</div><div class="hr-cell">Owner</div><div class="hr-cell">Due</div><div class="hr-cell">Audience</div><div class="hr-cell">Status</div><div class="hr-cell">Assigned</div><div class="hr-cell">Action</div></div>${state.training.map((t) => `<div class="hr-row training"><div class="hr-cell"><strong>${esc(t.title)}</strong></div><div class="hr-cell"><input data-training="${t.id}" data-prop="owner" value="${esc(t.owner)}" /></div><div class="hr-cell"><input type="date" data-training="${t.id}" data-prop="dueDate" value="${esc(t.dueDate)}" /></div><div class="hr-cell"><input data-training="${t.id}" data-prop="audience" value="${esc(t.audience)}" /></div><div class="hr-cell"><select data-training="${t.id}" data-prop="status"><option${t.status === 'Draft' ? ' selected' : ''}>Draft</option><option${t.status === 'Active' ? ' selected' : ''}>Active</option><option${t.status === 'Archived' ? ' selected' : ''}>Archived</option></select></div><div class="hr-cell"><span class="hr-pill">${assignedCount(t.title)}/${state.employees.length}</span></div><div class="hr-cell"><button type="button" class="hr-btn primary" data-action="assign-training" data-training="${t.id}">Assign</button></div></div>`).join('')}</section>`; }
  function policiesView() { return `${form('draftPolicy', [field('draftPolicy','title','Policy'), field('draftPolicy','category','Category'), field('draftPolicy','owner','Owner'), field('draftPolicy','status','Status',['Draft','Published','Archived']), field('draftPolicy','summary','Summary')], 'add-policy', 'Create policy')}<section class="hr-table"><div class="hr-row policy head"><div class="hr-cell">Policy</div><div class="hr-cell">Category</div><div class="hr-cell">Owner</div><div class="hr-cell">Status</div><div class="hr-cell">Summary</div><div class="hr-cell">Ack</div></div>${state.policies.map((p) => `<div class="hr-row policy"><div class="hr-cell"><strong>${esc(p.title)}</strong></div><div class="hr-cell"><input data-policy="${p.id}" data-prop="category" value="${esc(p.category)}" /></div><div class="hr-cell"><input data-policy="${p.id}" data-prop="owner" value="${esc(p.owner)}" /></div><div class="hr-cell"><select data-policy="${p.id}" data-prop="status"><option${p.status === 'Draft' ? ' selected' : ''}>Draft</option><option${p.status === 'Published' ? ' selected' : ''}>Published</option><option${p.status === 'Archived' ? ' selected' : ''}>Archived</option></select></div><div class="hr-cell"><input data-policy="${p.id}" data-prop="summary" value="${esc(p.summary)}" /></div><div class="hr-cell"><span class="hr-pill">${state.employees.filter((e) => e.policies && e.policies[p.title]).length}/${state.employees.length}</span></div></div>`).join('')}</section>`; }
  function body() { if (state.tab === 'onboarding') return onboardingView(); if (state.tab === 'training') return trainingView(); if (state.tab === 'policies') return policiesView(); return employeesView(); }
  function render() { if (!root) return; const active = state.employees.filter((e) => e.status === 'Active').length; const open = state.employees.filter((e) => !e.onboarding || !e.onboarding.Ready).length; root.dataset.hrLive = 'v6'; root.innerHTML = `<section class="hr-live-v6"><header class="workspace-header panel hr-hero"><div><p class="eyebrow">HR Portal</p><h1>HR Portal</h1><p>Employee records, movable onboarding board, training assignment, and policies.</p></div><div class="live-badge">Active HR</div></header>${state.message ? `<div class="hr-message">${esc(state.message)}</div>` : ''}<div class="hr-stats"><div class="hr-stat"><strong>Active</strong><b>${active}</b><span>${state.employees.length} employees</span></div><div class="hr-stat"><strong>Onboarding</strong><b>${open}</b><span>open cards</span></div><div class="hr-stat"><strong>Training</strong><b>${state.training.length}</b><span>modules</span></div><div class="hr-stat"><strong>Policies</strong><b>${state.policies.length}</b><span>records</span></div></div><nav class="hr-tabs"><button type="button" data-tab="employees" class="${state.tab === 'employees' ? 'active' : ''}">Employees</button><button type="button" data-tab="onboarding" class="${state.tab === 'onboarding' ? 'active' : ''}">Onboarding Board</button><button type="button" data-tab="training" class="${state.tab === 'training' ? 'active' : ''}">Training</button><button type="button" data-tab="policies" class="${state.tab === 'policies' ? 'active' : ''}">Policies</button></nav>${body()}</section>`; bind(); }
  function rerender(message) { if (message !== undefined) state.message = message; save(); render(); }
  function runAction(button) { const action = button.dataset.action; if (action === 'add-employee') { const d = state.draftEmployee; if (!d.name.trim()) return rerender('Enter an employee name first.'); const employee = { ...d, id: uid('emp'), onboarding: {}, training: {}, policies: {} }; stages.forEach((stage) => { employee.onboarding[stage] = false; }); state.employees.unshift(employee); state.draftEmployee = { ...emptyEmployee }; return rerender(`Created employee: ${employee.name}`); } if (action === 'add-training') { const d = state.draftTraining; if (!d.title.trim()) return rerender('Enter a training title first.'); state.training.unshift({ ...d, id: uid('tr') }); state.draftTraining = { ...emptyTraining }; return rerender(`Created training: ${d.title}`); } if (action === 'add-policy') { const d = state.draftPolicy; if (!d.title.trim()) return rerender('Enter a policy title first.'); state.policies.unshift({ ...d, id: uid('pol') }); state.draftPolicy = { ...emptyPolicy }; return rerender(`Created policy: ${d.title}`); } if (action === 'assign-training') { const t = state.training.find((item) => item.id === button.dataset.training); if (!t) return; state.employees.forEach((employee) => { employee.training = employee.training || {}; employee.training[t.title] = 'Assigned'; }); return rerender(`Assigned ${t.title} to ${state.employees.length} employees.`); } if (action === 'move-next' || action === 'move-prev') { const employee = state.employees.find((item) => item.id === button.dataset.emp); if (!employee) return; const idx = stages.indexOf(currentStage(employee)); setStage(employee, stages[action === 'move-next' ? Math.min(idx + 1, stages.length - 1) : Math.max(idx - 1, 0)]); return rerender(`Moved ${employee.name} to ${currentStage(employee)}.`); } }
  function bind() { root.querySelectorAll('[data-tab]').forEach((button) => { button.onclick = (event) => { event.preventDefault(); event.stopPropagation(); state.tab = button.dataset.tab; rerender(''); }; }); root.querySelectorAll('[data-action]').forEach((button) => { button.onclick = (event) => { event.preventDefault(); event.stopPropagation(); runAction(button); }; }); root.querySelectorAll('[data-draft]').forEach((input) => { input.oninput = input.onchange = () => { const parts = input.dataset.draft.split('.'); state[parts[0]][parts[1]] = input.value; save(); }; }); root.querySelectorAll('[data-emp]').forEach((input) => { input.oninput = input.onchange = () => { const row = state.employees.find((item) => item.id === input.dataset.emp); if (row) { row[input.dataset.prop] = input.value; save(); } }; }); root.querySelectorAll('[data-training]').forEach((input) => { if (input.matches('button')) return; input.oninput = input.onchange = () => { const row = state.training.find((item) => item.id === input.dataset.training); if (row) { row[input.dataset.prop] = input.value; save(); } }; }); root.querySelectorAll('[data-policy]').forEach((input) => { input.oninput = input.onchange = () => { const row = state.policies.find((item) => item.id === input.dataset.policy); if (row) { row[input.dataset.prop] = input.value; save(); } }; }); root.querySelectorAll('[data-emp-card]').forEach((card) => { card.ondragstart = (event) => { event.dataTransfer.setData('text/plain', card.dataset.empCard); event.dataTransfer.effectAllowed = 'move'; }; }); root.querySelectorAll('[data-stage]').forEach((stage) => { stage.ondragover = (event) => { event.preventDefault(); stage.classList.add('drag-over'); }; stage.ondragleave = () => stage.classList.remove('drag-over'); stage.ondrop = (event) => { event.preventDefault(); stage.classList.remove('drag-over'); const employee = state.employees.find((item) => item.id === event.dataTransfer.getData('text/plain')); if (employee) { setStage(employee, stage.dataset.stage); rerender(`Moved ${employee.name} to ${stage.dataset.stage}.`); } }; }); }
  function boot() { if (location.pathname.replace(/\/$/, '') !== PATH) return; css(); const workspace = document.querySelector('.workspace'); if (!workspace) return setTimeout(boot, 80); if (!state) state = load(); root = workspace; if (root.dataset.hrLive !== 'v6') render(); }
  window.addEventListener('popstate', () => setTimeout(boot, 80));
  new MutationObserver(() => setTimeout(boot, 80)).observe(document.documentElement, { childList: true, subtree: true });
  setTimeout(boot, 80);
  setTimeout(boot, 700);
})();
