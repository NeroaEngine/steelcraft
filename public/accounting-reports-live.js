(function () {
  // Reports are now rendered directly by src/AccountingPortal.jsx.
  // Keep this file as a no-op so older index.html references do not mutate the React layout.
  window.__accountingReportsLive = true;
}());
